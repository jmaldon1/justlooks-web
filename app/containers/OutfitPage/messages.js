/*
 * OutfitPage Messages
 *
 * This contains all the text for the OutfitPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OutfitPage';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'This is the OutfitPage container!',
    },
});
