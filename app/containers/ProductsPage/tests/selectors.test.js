// import { selectProductsPageDomain } from '../selectors';

describe('selectProductsPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
