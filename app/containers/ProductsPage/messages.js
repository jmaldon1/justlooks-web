/*
 * ProductsPage Messages
 *
 * This contains all the text for the ProductsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProductsPage';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'This is the ProductsPage container!',
    },
});
