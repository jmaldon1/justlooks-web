// import { selectOutfitsPageDomain } from '../selectors';

describe('selectOutfitsPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
