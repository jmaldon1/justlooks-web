/*
 * OutfitsPage Messages
 *
 * This contains all the text for the OutfitsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OutfitsPage';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'This is the OutfitsPage container!',
    },
});
