/*
 * ThumbnailGridList Messages
 *
 * This contains all the text for the ThumbnailGridList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ThumbnailGridList';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'This is the ThumbnailGridList component!',
    },
});
