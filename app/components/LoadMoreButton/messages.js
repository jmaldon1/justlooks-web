/*
 * LoadMoreButton Messages
 *
 * This contains all the text for the LoadMoreButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LoadMoreButton';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'This is the LoadMoreButton component!',
    },
});
